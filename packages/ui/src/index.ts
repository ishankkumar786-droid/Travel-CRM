/**
 * @travel/ui
 * Shared UI component library for the Travel Marketplace platform.
 *
 * Note: Business-specific components live in apps/admin-crm/components.
 * This package exports only truly generic, reusable primitives.
 */

// This package is a placeholder for shared components across
// multiple frontend apps (admin-crm, agency-portal, customer-web).
// Phase 1: Export nothing — apps use their own local component trees.

export type {} from './types';
